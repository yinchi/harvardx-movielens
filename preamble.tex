\usepackage{booktabs}

\hypersetup{
    colorlinks = true,
    linkcolor = blue,
    urlcolor = blue,
}

% Enable support for the <- ligature by clearing the no-ligature list
\makeatletter
\def\verbatim@nolig@list{}
\makeatother
